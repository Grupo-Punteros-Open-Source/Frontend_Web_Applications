import { CustomerVehicleEntity } from './customer-vehicle.entity';

describe('VehicleEntity', () => {
  it('should create an instance', () => {
    expect(new CustomerVehicleEntity()).toBeTruthy();
  });
});
